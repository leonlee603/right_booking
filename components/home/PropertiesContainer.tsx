export default function PropertiesContainer({
  category,
  search,
}: {
  category?: string;
  search?: string;
}) {
  console.log(category, search);
  return (
    <div>PropertiesContainer</div>
  )
}